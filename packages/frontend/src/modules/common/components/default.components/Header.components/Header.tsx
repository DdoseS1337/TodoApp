import React from 'react';

import { HeaderContainer, HeaderWrapper, Heading } from './Header.styled';

export const Header = () => (
  <HeaderContainer>
    <HeaderWrapper>
      <Heading>Todo project</Heading>
    </HeaderWrapper>
  </HeaderContainer>
);
